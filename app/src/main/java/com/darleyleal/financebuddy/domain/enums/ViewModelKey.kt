package com.darleyleal.financebuddy.domain.enums

enum class ViewModelKey {
    CATEGORY_EXPENSES,
    CATEGORY_INCOMES,
    HOME,
    ANALYTICS,
    START,
    INSERT,
    NEW_CATEGORY,
    BALANCE
}